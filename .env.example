# Budget App - Environment Variables
# Copy this file to .env.local and fill in your values

# ===========================================
# DATABASE CONFIGURATION
# ===========================================

# Option 1: Turso Cloud (recommended for Vercel deployment)
# Sign up at https://turso.tech (free tier: 500 DBs, 9GB storage)
# Create database: turso db create budget-app
# Get URL: turso db show budget-app --url
# Get token: turso db tokens create budget-app
TURSO_DATABASE_URL=libsql://your-database-name.turso.io
TURSO_AUTH_TOKEN=your-auth-token

# Option 2: Local SQLite file (for self-hosting with Docker)
# Just set the URL to a file path, no auth token needed
# TURSO_DATABASE_URL=file:/app/data/budget.db
# TURSO_AUTH_TOKEN=

# Option 3: Leave both empty for in-memory SQLite (development only)
# Data will NOT persist across restarts!

# ===========================================
# AUTHENTICATION (Choose One)
# ===========================================

# OPTION A: Email-based Authentication (Recommended for hosted instances)
# Magic link + OTP via email - no passwords to remember!
# Get your API key at https://resend.com (free: 100 emails/day)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# Email sender address (requires verified domain on Resend, or use default)
# EMAIL_FROM=Budget App <login@yourdomain.com>

# Your app's public URL (for magic links)
APP_URL=https://your-domain.com

# OPTION B: Password Authentication (For self-hosted private instances)
# If RESEND_API_KEY is not set, the app falls back to password auth
# Set via /setup page on first run, or use env var:
# APP_PASSWORD=your-secure-password

# ===========================================
# USER LIMITS (For Hosted Instances)
# ===========================================

# Maximum number of users allowed (hosted instance cost control)
# Personal use default: 5 users (you + family/friends)
# - Set to limit: USER_CAP=5 (personal) or USER_CAP=50 (small team)
# - Set to unlimited: USER_CAP=0 or USER_CAP=unlimited
# - Default: 5 users
USER_CAP=5

# ===========================================
# EMAIL LIMITS (Resend Free Tier)
# ===========================================

# Daily email limit (Resend free tier: 100 emails/day, 3000/month)
# Set to 0 or leave empty for unlimited (paid plans)
# Default: 100 (matches Resend free tier)
DAILY_EMAIL_LIMIT=100

# ===========================================
# APPLICATION MODE
# ===========================================

# Set to 'true' for public demo deployment (only /demo page accessible)
# Leave empty or 'false' for normal operation
DEMO_MODE=false

# Allow demo access on private instances (set to 'true' to enable /demo page)
# For self-hosted private instances, leave this empty or 'false' to block demo access
ALLOW_DEMO=false

# ===========================================
# OPTIONAL: External APIs
# ===========================================

# Exchange rates API (for multi-currency support)
# Get free API key at https://exchangeratesapi.io
# EXCHANGE_RATES_API_KEY=your-api-key
